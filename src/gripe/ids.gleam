/// Starts with "acct_"
pub type AccountId =
  String

/// Starts with "txi_" or "atxi_"
pub type TaxIdId =
  String

/// Starts with "ca_"
pub type ApplicationId =
  String

/// Starts with "di_"
pub type DiscountId =
  String

/// Starts with "promo_"
pub type PromotionCodeId =
  String

/// Starts with "src_"
pub type SourceId =
  String

/// User-defined, can be any arbitrary string
pub type CouponId =
  String
